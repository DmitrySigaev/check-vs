# defined to 1 if subunit is enabled
ENABLE_SUBUNIT=0
export ENABLE_SUBUNIT
EXEEXT=.exe
export EXEEXT
HAVE_FORK=0
export HAVE_FORK

# path of the tests directory
if [ x"F:\\ProjectExt\\check-vs\\check\\tests" != x"." ]; then
    if [ -z "1" -o "1" != "1" ]; then
       SRCDIR="F:\\ProjectExt\\check-vs\\check\\tests/"
    else
       SRCDIR="F:\\ProjectExt\\check-vs\\check\\tests\\"
    fi
else
    SRCDIR=""
fi

export SRCDIR
